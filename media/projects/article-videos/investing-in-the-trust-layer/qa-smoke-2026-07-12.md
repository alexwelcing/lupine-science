# Live QA smoke test — Investing in the Trust Layer

Tested: 2026-07-12 06:54 UTC
Target: https://lupine.science

## Site smoke suite

Command (repository root `/home/alex/Dev/lupine/lupine-science`):

`npm run smoke`

Result: PASS (exit code 0)

- 14 pages checked
- 352 linked resources checked
- All live smoke checks passed across 1 target

## Video endpoint

URL:

`https://lupine.science/videos/investing-in-the-trust-layer.mp4?qa=20260712T065448Z`

Observed:

- HTTP status: `200`
- Content-Type: `text/html; charset=utf-8` (incorrect; expected an MP4 media type such as `video/mp4`)
- Response size: 84,245 bytes
- `file(1)`: HTML document, Unicode text, UTF-8 text
- First 16 response bytes (hex): `3c 21 64 6f 63 74 79 70 65 20 68 74 6d 6c 3e 0a`, which decodes to an HTML doctype prefix (`<!doctype html>`)
- Cloudflare cache status: `MISS`, so the cache-busting request still reached a non-video response

## Verdict

FAIL. The general live smoke suite passes, and the requested video URL returns HTTP 200, but it serves the HTML fallback page rather than MP4 media. The endpoint therefore does not have the required content type or media payload.
