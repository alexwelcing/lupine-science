# Investing in the Trust Layer — QA result

Timestamp: 2026-07-12T01:46:24Z
Task: `t_b69a5985`

## Live smoke test

Command: `npm run smoke` from `/home/alex/Dev/lupine/lupine-science`

Result: PASS (exit 0)

```text
Smoke-testing https://lupine.science (5 attempt(s), 10000ms delay)
  PASS: 14 pages and 352 linked resources
All live smoke checks passed across 1 target(s).
```

Note: Running the command from `media/projects/article-videos` first returned `Missing script: "smoke"`; the smoke script is defined at the repository root.

## Stable video URL

URL: `https://lupine.science/videos/investing-in-the-trust-layer.mp4`

Result: FAIL

A full GET returned:

```text
HTTP=200
CONTENT_TYPE=text/html; charset=utf-8
SIZE=84245
URL=https://lupine.science/videos/investing-in-the-trust-layer.mp4
file: HTML document, Unicode text, UTF-8 text
```

A range GET independently returned HTTP 206, `Content-Type: text/html; charset=utf-8`, and an HTML payload.

Expected: HTTP 200 with `Content-Type: video/mp4` and an MP4 payload.

Conclusion: the general live smoke suite passes, but the stable video link still resolves to the site's HTML fallback rather than the video. The task cannot pass until the MP4 is present in the live deployment/routing path.
