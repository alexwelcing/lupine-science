# HyperFrames reference clone (large, can be re-cloned)
hyperframes-ref/

# FAL engine prototype test artifacts
prototype-fal-test/

# Rendered MP4 outputs (large binaries; keep only final web copies in public/videos)
prototype-*/renders/
prototype-*/node_modules/

# TTS virtual environment
.venv-tts/

# Audio working files larger than final mix
prototype-*/audio/narration.vtt

# Visual regression diffs and re-runs are ephemeral; baselines are committed.
media/projects/article-videos/components/*/diffs/
media/projects/article-videos/components/*/snapshots/
