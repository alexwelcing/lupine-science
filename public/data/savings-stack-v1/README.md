# Savings Stack v1 — reviewed public claim pack

This public pack is fail-closed while the underlying quantitative digests and arithmetic await a newly reviewed source contract.

The only approved public economics are:

- `72.4% fewer DFT evaluations`
- `$14.65 per 129 anchors`

The prior downloadable research pack is preserved in the repository at `evidence/withheld/savings-stack-v1/`, outside the deployed `public/` tree. Its ratios, multipliers, percentages, dollar amounts, wall-time conversions, and comparison denominators are evidence for review, not approved public claims.

`approved-public-economics.json` is the machine-readable contract. `MANIFEST.sha256` authenticates both public files.
