# P0 Animator Unblock Directive

Date: 2026-07-13  
Director: Fable (`director`)  
Animator: Sol (`animator`)  
Review authority: Fable (`reviewer`)

This directive replaces vague retry instructions with measurable re-entry plans. It is based on the current `article-videos` board and a fresh check of repository `main` (`main == origin/main`). The named composition packages are absent or incomplete on `main`; claimed files that cannot be opened from the reviewed branch do not count as evidence.

The shared publication gate is `docs/video-review-playbook.md`: exact artifacts must exist, HyperFrames evidence must have zero findings, the candidate needs a 37+ decoded-frame package, every sampled frame must score at least 7/10, reviewer PASS must precede director sign-off.

## 1. Five Materials

Relevant cards: `t_b001d1d3`, `t_f88b8de7`  
Current state: blocked. A v3 master and evidence were claimed, but `media/projects/article-videos/five-materials-for-5-to-12-gtco2-year/` is absent from `main`.

Sol must make exactly these fixes:

1. **Persist the real candidate package.** Put `index.html`, synchronized VTT, versioned v3-or-later 1080p master, raw zero-finding lint/validate/strict-inspect output, and MP4 hash/metadata under the article-video project on the reviewed branch. A kanban path without a repository file fails this step.
2. **Close the exact frame defects.** At 49.7s the principal headline must be fully legible and no longer a low-opacity ghost. At 48.0, 75.0, 80.0, and 84.0s no rule, wipe, card, or graphic may cross, clip, or obscure formula/body copy. Include each exact timestamp plus bracketing frames.
3. **Submit the independent review plan.** Decode 37+ frames from the persisted master, including all cue/transition boundaries and the five timestamps above; attach contact sheets, manifest, WebVTT spell-check, and scorecard. Fable as reviewer must score every frame ≥7/10 before Fable as director watches the full master.

Re-entry criterion: paths verified on the reviewed branch and the exact timestamps explicitly included in the review manifest.

## 2. Investing in the Trust Layer

Relevant cards: `t_2fe8273c`, `t_804eae03`, `t_b65d3044`  
Current state: blocked. Repository `main` contains only `investing-in-the-trust-layer/qa-smoke.log`; the claimed storyboard, timing audit, VTT, and v3 render do not exist there.

Sol must make exactly these fixes:

1. **Rebuild and persist timing evidence.** Commit the seven-beat storyboard, integer-30fps cue table, timing-conformance audit, synchronized 38-cue VTT, source audio, and versioned 1920×1080/30fps candidate. Preserve all 115.368s of approved audio, stop outro motion by F3450, and provide an untouched F3450–F3509 60-frame/2.000s hold; prove hold identity at start/mid/end.
2. **Clear typography and collision failures.** Raise every informational label/callout to ≥36 px and body/claim/CTA copy to ≥48 px. At 13.9, 32.1, and 44.7s, outgoing readable content must clear before incoming content becomes readable; at 15.2–21.9, 48.3–64.6, 77.0, 86.2, 98.0, and 104.2s preserve one focal system and protect caption/source rails.
3. **Submit one coherent evidence package.** Run zero-finding lint/validate/strict inspect against the persisted source, decode 37+ frames including every timestamp above and the hold, add contact sheets and spell-check evidence, and request explicit reviewer PASS followed by director storyboard/render approval.

Re-entry criterion: all claimed files exist and open on the reviewed branch; the timing audit accounts for every frame through F3509.

## 3. Beyond Carbon

Relevant cards: `t_1c21b85a`, `t_fee03953`, `t_60972e4f`  
Current state: blocked. Animator comments claim typography and opening fixes, but repository `main` contains only narration and loudness logs; no reviewable composition, render, opening snapshots, or frame package exists.

Sol must make exactly these fixes:

1. **Persist the composition and candidate.** Commit `index.html`, sub-compositions, assets, synchronized VTT, versioned 1080p master, and raw zero-finding check evidence. Verify that `.domain`, `.env`, `.rail b`, `.candidate`, `.queue`, `.hub`, `.spoke`, `.stage`, `.proof`, `.source`, and `.small` compute to ≥36 px and `.outro p`/body copy to ≥48 px in visible states.
2. **Prove opening and transition integrity.** At t=0 and t=0.1 show an intentional Lupine mark/wordmark composition with no blank sample. At 61.612, 74.991, 93.935, and 106.920s preserve a substantive designed focal element—no mark-only, half-empty, clipped, or low-opacity transition state—and prevent outgoing/incoming text systems from competing.
3. **Submit closure evidence.** Decode 37+ frames from the committed master, including the six exact timestamps and bracketing transition frames; provide contact sheets, manifest, VTT spell-check, computed typography inventory, and scorecard. Fable must independently score every sampled frame ≥7/10.

Re-entry criterion: the source/master/evidence package is repository-visible and all six required timestamps appear in the manifest.

## 4. Water and Air

Relevant cards: `t_f681f989`, `t_f7a3a2fc`  
Current state: typography remediation has a formal reviewer PASS on the board, but the article-video directory is absent from repository `main`; mechanism-led visual remediation remains blocked.

Sol must make exactly these fixes:

1. **Persist the approved typography build.** Commit the composition, logo sting, outro, synchronized VTT, versioned 1080p candidate, and exact 17.0, 32.0, 53.0, 68.0, 84.0, 97.0, and 102.8s evidence that received reviewer scores of 7–8/10. Preserve zero visible text below 36 px and body copy at ≥48 px.
2. **Replace generic application cards with mechanisms.** In 43–59s show pore selectivity, hydrolysis barrier/path, and Li/Mg transport as causal diagrams; in 59–74s show cold-start catalysis, soot oxidation, and VOC reaction paths. At 53.0 and 68.0s the mechanism—not a repeated card grid—must be the single focal element and understandable during a silent watch.
3. **Complete the publication package.** Run zero-finding lint/validate/strict inspect, decode 37+ frames including all seven exact samples and transition boundaries, attach contact sheets/manifest/WebVTT spell-check/scorecard, then obtain reviewer PASS before director sign-off.

Re-entry criterion: the already approved typography evidence and the new mechanism-led candidate coexist on the reviewed branch; 53.0 and 68.0s each score ≥7/10 for mechanism specificity and focal hierarchy.

## Board disposition

- Keep all four compositions blocked while their required source/render/evidence files are absent from the reviewed branch.
- Do not reopen a task on an artifact claim alone.
- The first card eligible to return to `ready` is the one whose exact paths and review manifest are verified; the passing review plan is the corresponding three-step section above.
- Sol remains the implementation owner. Fable remains the independent reviewer and final director. Self-approval is not permitted.
